{
  "permissions": {
    "allow": [
      "Bash(npm install *)",
      "PowerShell(npm install --no-audit --no-fund 2>&1)",
      "PowerShell(Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.InterfaceAlias -notmatch \"Loopback|Bluetooth\" -and $_.IPAddress -notmatch \"^169\" } | Select-Object InterfaceAlias, IPAddress)"
    ]
  }
}
